# ---------------------------------------------------------------------
# XAI contestation project — Python dependencies
# Create + activate the venv, then:  pip install -r requirements.txt
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
# ---------------------------------------------------------------------

# --- Core data / audit stack (notebooks 01 & 02) ---------------------
pandas>=2.2
numpy>=1.26
matplotlib>=3.8
pyarrow>=15.0          # parquet read/write (translation cache, exports)
openpyxl>=3.1          # xlsx export of the review sample

# --- Jupyter (to run the notebooks) ----------------------------------
jupyterlab>=4.0
ipykernel>=6.29
ipython>=8.20

# --- Translation stack (notebook 02: PL -> EN, local/offline) --------
# Verified installing on Python 3.14 (torch 2.13, transformers 5.x). If a
# future torch has no wheel for your interpreter, either run the audit
# without translation or pin a Python 3.11–3.13 venv.
transformers>=4.40
torch>=2.2
sentencepiece>=0.2     # required by the opus-mt tokenizer
sacremoses>=0.1        # required by the opus-mt tokenizer

# --- LLM auto-coder (notebook 03; optional) --------------------------
# Only needed to run the real Anthropic backend (BACKEND="anthropic").
# The notebook's default offline mock backend needs nothing here.
# Sends episode text to an external API — gate on licence/consent review.
anthropic>=0.60        # pulls in pydantic; auth via ANTHROPIC_API_KEY or `ant auth login`

# --- Detection benchmark (notebook 06) ---
scikit-learn>=1.3    # TF-IDF, logistic regression, GroupKFold, metrics
